% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{createtree}
\alias{createtree}
\title{Create a Phylogenetic Tree from Taxonomy Profile}
\usage{
createtree(
  tax.profile,
  index = 1,
  header = FALSE,
  delimiter = "\\\\|",
  node.size.scale = 1,
  node.size.offset = 1
)
}
\arguments{
\item{tax.profile}{Either a character string specifying the path to a taxonomy profile file or a data frame containing the taxonomy profile.}

\item{index}{An integer specifying the column index of the taxonomy string in the profile. Default is 1.}

\item{header}{A logical value indicating whether the taxonomy profile file has a header. Default is FALSE.}

\item{delimiter}{A character string specifying the delimiter used to separate taxonomic levels in the taxonomy string. Default is '\\|'.}

\item{node.size.scale}{A numeric value specifying the scaling factor for node sizes based on relative abundance. Default is 1.}

\item{node.size.offset}{A numeric value specifying the offset for node sizes. Default is 1.}
}
\value{
Returns a \code{treedata} object that includes the phylogenetic tree and associated data such as node sizes and classifications.
}
\description{
The \code{createtree} function constructs a phylogenetic tree object from a taxonomy profile. The tree includes node sizes based on relative abundance and classifies nodes into taxonomic levels.
}
\details{
The function reads a taxonomy profile, processes it to remove unclassified taxa, and splits the taxonomy strings into different levels. It then constructs a phylogenetic tree, assigns node sizes based on relative abundance, and classifies nodes into taxonomic levels.
}
\examples{
\dontrun{
# Example usage
tax.profile <- "path/to/taxonomy_profile.txt"
tree <- createtree(tax.profile, index = 1, header = FALSE, delimiter = '\\\\|')
}

}
